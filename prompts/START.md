# Start prompt

```text
/plan
Use AGENTS.md. Inspect the scaffold and Phase 0 in docs/IMPLEMENTATION_PLAN.md; open other docs only as needed. Run the four Done checks. Report blockers and propose the smallest first behavior with its acceptance criteria. Wait for approval before RED; then run the sequential TDD loop, one behavior at a time.
```
